module Main where
import System.Environment

import Advent
import Solutions.Exports

adventCLI :: IO ()
adventCLI = do
    return ()
main :: IO ()
main = do
    -- n <- head <$> getArgs 
    -- fp <- map read . lines <$> readFile "inputs/"<> show n <> ".txt"
    return ()
    -- let f = case n of
    --           "1" -> twoSum'
    --           "1a" -> threeSum'
    --           "2" -> verify
              
              
s = "1-3 a: abcde\n1-3 b: cdefg\n2-9 c: ccccccccc"
